// Recovered banner rotation removed - see project history for the removal
// decision. Left as empty arrays (rather than deleting the file) so
// site.js's existing typeof/length checks continue to no-op safely.
const RECOVERED_BANNERS = [];
const RECOVERED_MINIBANNERS = [];
